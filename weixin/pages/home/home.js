const app = getApp();

Page({
    data: {
        gData: null,
        results: {}
    },

    onLoad: function () {
        
    },

    onShow: function () {
        // app.helper.waitUserSid(this.getApiData);
    }
});
